# 已迁移

本旧 H5 样例不再作为主线使用。

新的示例入口将使用：

```text
examples/01_minimal_harness/
```

当前仓库目标是：

```text
定义一套文档驱动的 AI 产品交付方法论，
提供可复用的 harness 模板和 checklist，
让非技术人能学会并实际使用。
```

H5、小程序、App 都只是后续应用场景，不再作为方法论主线。
