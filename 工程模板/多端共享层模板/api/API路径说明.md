# API 路径说明

多端 API 路径必须来自接口契约，不允许各端自行发明。

当前默认前缀：

```text
/api/v1
```

常见路径：

```text
会话接口：/api/v1/session
状态接口：/api/v1/status
任务接口：/api/v1/video-tasks
```

规则：

- 路径常量应由接口契约生成或手工同步。
- 修改路径前必须先修改接口契约。
- 所有端必须使用同一套路径定义。
