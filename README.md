# 阶段受控的 AI 产品交付协议

Document-Driven AI Product Delivery Harness

## 1. 仓库定位

本仓库用于沉淀一套约束 AI 作为生产力的产品交付系统。

它的核心目标是：

```text
让人提出产品意图，
让系统把意图变成结构化契约，
让 AI 在工程模板和任务包内执行，
让门禁检查 AI 是否越界，
让结果以人能决策的方式反馈回来。
```

## 2. 先读总览

先读：

```text
文档/控制系统/00_系统总览.md
```

这个文件用总分结构说明：

```text
总目标：约束 AI 作为生产力
分路径：使用者主线、AI 主线、工程主线、治理主线
文件地图：当前每类内容放在哪里
收敛规则：什么不要再加
下一步：补机器门禁，而不是继续堆文档
```

## 3. 总路径

```text
人说出产品想法
↓
系统判断当前阶段
↓
系统把想法转成结构化问题、选项和文档
↓
人确认产品方向和界面预期
↓
系统冻结阶段性契约
↓
AI 在工程模板和任务包内执行
↓
机器门禁检查是否越界
↓
失败时翻译成人话选项
↓
人做产品决策
↓
系统进入下一轮交付
```

## 4. 四条主线

```text
使用者主线：想法 → 选择 → 原型确认 → 验收
AI 主线：读上下文 → 判阶段 → 判边界 → 执行任务
工程主线：文档 → 原型 → 契约 → 模板 → 编码 → 验证
治理主线：文档约束 → 结构化契约 → 机器门禁 → 人话反馈
```

## 5. 仓库结构

```text
文档/        方法论、工程基线、控制系统、操作手册、教学化内容
样板/        可复用空白样板、控制系统元数据样板
工程模板/    可直接复制使用的 Web、H5、微信小程序、iOS、后端多类独立工程模板
检查清单/    可执行检查清单
指令/        AI / Agent / Review 指令
案例/        填好的案例
```

## 6. 从哪里开始

请从这里开始：

```text
文档/控制系统/00_系统总览.md
文档/说明.md
```

## 7. 当前工程基线

```text
Web 后台：Vue 3 + Vite + TypeScript + Vue Router + Pinia + Element Plus
H5：Vue 3 + Vite + TypeScript + Vant
小程序：原生微信小程序 + TypeScript + Vant Weapp
iOS：SwiftUI + MVVM
后端：Java + Spring Boot + MyBatis-Plus + Flyway 单体应用，不使用 Spring Cloud
数据库：MySQL 优先；主键 32 位字符串；每表 7 要素；时间 trigger 维护
码值：数据库码值是源头，后端常量一致，接口契约记录，前端只翻译不改码
部署：本地开发优先，微信云托管按需适配
多端原则：多个工程，独立建设；接口一致，代码不共享
```

## 8. 当前状态

```text
控制协议已建立。
机器门禁待实现。
```

下一步优先补最小门禁脚本，而不是继续横向新增规范文档。

## 9. 最重要的一句话

```text
文档表达意图，契约收束歧义，模板固定边界，门禁阻止越界，报告帮助决策。
```
