# 前端工程检查清单

## 1. 技术栈

- [ ] 使用 Vue 3
- [ ] 使用 Vite
- [ ] 使用 TypeScript
- [ ] 使用 Vue Router
- [ ] 状态管理使用 Pinia
- [ ] 使用 ESLint 和 Prettier

## 2. 目录结构

- [ ] 页面放在 `src/pages/`
- [ ] 组件放在 `src/components/`
- [ ] API 调用放在 `src/services/`
- [ ] 状态放在 `src/stores/`
- [ ] 类型放在 `src/types/`
- [ ] 样式放在 `src/styles/`

## 3. 编码约束

- [ ] 页面中没有直接散落 fetch
- [ ] API 地址没有硬编码
- [ ] 字段来自数据规范
- [ ] 页面状态包含 loading / empty / success / error
- [ ] 样式符合设计规格

## 4. 验证

- [ ] `npm install` 可执行
- [ ] `npm run dev` 可启动
- [ ] `npm run build` 可通过
- [ ] 关键页面可访问
- [ ] 关键交互可完成

## 5. 结论

```text
是否通过：通过 / 有条件通过 / 不通过
必须修复：
是否可以进入下一任务：是 / 否
```
