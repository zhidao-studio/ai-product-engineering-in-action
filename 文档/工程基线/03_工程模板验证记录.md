# 工程模板验证记录

## 1. 目的

本文件用于记录工程模板是否已经真实运行验证。

只有文件存在不代表模板可用。

模板必须经过安装、检查、测试、构建或对应平台打开验证，才能作为稳定工程基座使用。

## 2. 验证状态定义

```text
未验证：模板文件已生成，但没有实际运行。
缺件已补齐待验证：已修复明显缺失文件，但尚未执行真实命令验证。
部分通过：部分命令已运行，仍有缺口。
通过：核心命令或平台打开验证已通过。
失败：执行过验证但未通过，需要修复。
```

## 3. Web 后台模板工程

路径：

```text
工程模板/Web后台模板工程/
```

待验证命令：

```text
npm install
npm run lint
npm run format:check
npm run typecheck
npm run test
npm run build
npm run check
```

当前状态：未验证。

待确认事项：

```text
package-lock.json 是否需要提交
Vite / Pinia 当前版本是否与模板依赖兼容
```

## 4. H5 模板工程

路径：

```text
工程模板/H5模板工程/
```

待验证命令：

```text
npm install
npm run lint
npm run format:check
npm run typecheck
npm run test
npm run build
npm run check
```

当前状态：缺件已补齐待验证。

已修复：

```text
补齐 eslint.config.js
补齐 src/components/ 目录占位
补齐 src/stores/index.ts
将 ESLint 命令调整为 flat config 兼容的 eslint .
```

遗留风险：

```text
尚未真实执行 npm install / npm run check。
依赖版本是否全部兼容仍需实跑验证。
```

## 5. 微信小程序模板工程

路径：

```text
工程模板/微信小程序模板工程/
```

待验证事项：

```text
npm install
npm run typecheck
npm run test
微信开发者工具打开
构建 npm
页面预览
```

当前状态：未验证。

待确认事项：

```text
sitemap.json 是否需要补齐
Vant Weapp npm 构建说明是否需要进一步明确
游客模式 appid 是否满足新用户体验
```

## 6. iOS 模板工程

路径：

```text
工程模板/iOS模板工程/
```

待验证事项：

```text
使用 Xcode 新建 SwiftUI App
将 Sources/App/ 文件复制进 App target
将 Tests/ 文件复制进测试 target
将 Podfile 放到 .xcodeproj 同级目录
pod install
使用 .xcworkspace 打开工程
xcodebuild 构建
示例页面预览
```

当前状态：缺件已补齐待验证。

已修复：

```text
iOS 基线从 Swift Package Manager 调整为 CocoaPods
补齐 工程模板/iOS模板工程/Podfile
README 明确使用 .xcworkspace 打开工程
门禁脚本支持 Podfile / Podfile.lock 边界检查
依赖白名单脚本支持 pod 'Name' 识别
```

遗留风险：

```text
当前仍不是可直接打开的完整 Xcode 工程。
尚未真实执行 pod install / xcodebuild。
Podfile target 名称需要与实际 Xcode target 保持一致。
```

## 7. 后端模板工程

路径：

```text
工程模板/后端模板工程/
```

待验证命令：

```text
mvn test
mvn package
mvn spring-boot:run
```

待验证接口：

```text
GET /api/v1/status
POST /api/v1/session
GET /api/v1/video-tasks
POST /api/v1/video-tasks
```

当前状态：缺件已补齐待验证。

已修复：

```text
补齐 src/main/resources/application.yml
默认使用 H2 内存库，方便本地启动和测试
将 H2 调整为 runtime 依赖，支持 spring-boot:run 默认启动
修正 README 中不存在的 ./mvnw 命令，改为 mvn 命令
```

遗留风险：

```text
尚未真实执行 mvn test / mvn package / mvn spring-boot:run。
Flyway migration、Mapper 扫描、样例接口是否完全可用仍需实跑验证。
```

## 8. 记录模板

每次验证后追加：

```text
验证时间：
验证人：
模板路径：
执行命令：
结果：通过 / 失败
失败原因：
修复记录：
遗留风险：
```

## 9. 使用规则

如果模板状态是“未验证”或“失败”，不得宣称该模板已经生产可用。

如果模板状态是“缺件已补齐待验证”，只能说明已修复明显缺件，不得宣称真实构建通过。

可以用于方法论演示，但进入真实项目之前必须先验证。
